<h1>Item Types</h1>
<p>These are used as enumeration for items model. This type of enumeration allows for updatable content without code commits.</p>
<%= link_to 'New Item Type', new_item_type_path, class: 'btn btn-primary' %>

<table class="table">
  <thead>
    <tr>
      <th>image</th>
      <th>Name</th>
      <th>image_url</th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @item_types.each do |item_type| %>
      <tr>
        <td><%= image_tag item_type.image_url, size: "160x160" %></td>
        <td><%= item_type.name %></td>
        <td><%= item_type.image_url %></td>
        <td>
          <%= link_to 'Show', item_type %>
          <%= link_to 'Edit', edit_item_type_path(item_type) %>
          <%= link_to 'Destroy', item_type, method: :delete, data: { confirm: 'Are you sure?' } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

