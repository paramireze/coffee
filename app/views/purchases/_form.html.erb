<%= form_for(purchase) do |f| %>
  <% if purchase.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(purchase.errors.count, "error") %> prohibited this purchase from being saved:</h2>

      <ul>
      <% purchase.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :purchaseDate %>
    <%= f.datetime_select :purchaseDate %>
  </div>

  <div class="field">
    <%= f.label :deleted %>
    <%= f.check_box :deleted %>
  </div>

    <%= f.collection_select(:item_id, Item.all, :id, :price_with_store_with_brand) %><br />
    <%= f.collection_check_boxes(:user_ids, User.all, :id, :name) do |user|%>
      <%= user.label { user.check_box + user.text }  %><br />
    <%  end %>
    <div class="actions">
      <%= f.submit %>
    </div>

<% end %>
